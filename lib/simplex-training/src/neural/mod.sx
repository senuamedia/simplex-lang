// Neural module: Dual-mode neural gates for training/inference
//
// This module provides differentiable neural gates that operate in different
// modes for training (soft, differentiable) and inference (hard, discrete).
//
// # Key Components
//
// - `gate`: Dual-mode gates with temperature control
// - `soft_logic`: Differentiable logic operations (AND, OR, NOT, XOR)
// - `temperature_attention`: Temperature-aware attention mechanisms
//
// # Training vs Inference
//
// In training mode, gates use soft outputs (sigmoid/softmax) that allow
// gradient flow. In inference mode, gates produce hard binary decisions
// for efficiency. The Hybrid mode uses straight-through estimators.

pub mod gate;
pub mod soft_logic;
pub mod temperature_attention;

pub use gate::{
    DualGate,
    GateMode,
    GumbelSoftmax,
    StraightThroughEstimator,
};

pub use soft_logic::{
    SoftLogic,
    SoftLogicGate,
    LogicOp,
};

pub use temperature_attention::{
    TemperatureAttention,
    LearnableTemperature,
    AttentionConfig,
};
