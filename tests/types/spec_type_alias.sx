// Test type alias syntax
// type Name = ExistingType;

type Count = i64;
type Number = i64;

fn add_counts(a: Count, b: Count) -> Count {
 a + b
}

fn main() -> i64 {
 let x: Count = 10;
 let y: Number = 32;
 let result: i64 = add_counts(x, y);
 if result == 42 {
  return 0;
 }
 1
}
