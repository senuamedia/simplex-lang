// Test basic generic function syntax
// For now, generics are treated as i64 (no real monomorphization yet)
fn identity<T>(x: T) -> T {
 x
}

fn main() -> i64 {
 let result: i64 = identity(42);
 if result == 42 {
  return 0;
 }
 1
}
