// test_import.sx - Test file for module system
// Tests: use, mod, pub visibility

// Import from the mathlib module
use mathlib;

fn main() -> i64 {
 println("=== Module System Test ===");
 println("");

 // Test imported functions
 println("--- Testing imported functions ---");
 let sum: i64 = add(10, 5);
 print("add(10, 5) = ");
 print_i64(sum);
 println("");

 let diff: i64 = subtract(10, 5);
 print("subtract(10, 5) = ");
 print_i64(diff);
 println("");

 // Test imported struct
 println("");
 println("--- Testing imported struct ---");
 let p1: Point = point_new(0, 0);
 let p2: Point = point_new(3, 4);
 let dist_sq: i64 = point_distance_squared(p1, p2);
 print("distance_squared((0,0), (3,4)) = ");
 print_i64(dist_sq);
 println(" (expected: 25)");

 // Test imported enum (just construction, skip match for now)
 println("");
 println("--- Testing imported enum ---");
 let red: i64 = 0; // Color::Red
 let green: i64 = 1; // Color::Green
 let blue: i64 = 2; // Color::Blue

 print("Red = ");
 print_i64(red);
 println("");

 print("Green = ");
 print_i64(green);
 println("");

 print("Blue = ");
 print_i64(blue);
 println("");

 println("");
 println("=== Module System Test Complete! ===");
 0
}
