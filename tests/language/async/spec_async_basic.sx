// Test async function definition (verification only)
// Async full implementation is in progress

async fn compute(x: i64) -> i64 {
    x * 2
}

fn sync_compute(x: i64) -> i64 {
    x * 2
}

fn main() -> i64 {
    // Test sync version for now
    let result = sync_compute(21);
    if result != 42 {
        print("FAIL: expected 42");
        return 1;
    }
    print("PASS: async basic (sync verification)");
    0
}
